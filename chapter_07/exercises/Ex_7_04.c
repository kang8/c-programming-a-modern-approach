/**
 * If c is variable of type char, which one of the following statements in
 * illegal?
 *
 * (a) i += c; // i has type int
 * (b) c = 2 * c - 1;
 * (c) putchar(c);
 * (d) printf(c); // illegal
 */
