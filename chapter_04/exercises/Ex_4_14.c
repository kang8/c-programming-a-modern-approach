/**
 * Supply parentheses to show how a C compiler would interpret each of the
 * following expressoins.
 *
 * (a) a * b - c * d + e // (((a * b) - (c * d)) + e)
 * (b) a / b % c / d     // (((a / b) % c) / d)
 * (c) - a - b + c - + d // ((((-a) - b) + c) - (+d))
 * (d) a * -b / c - d    // (((a * (-b)) / c) - d)
 */
