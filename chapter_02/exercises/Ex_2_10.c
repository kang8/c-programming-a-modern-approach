/**
 * In the ../dweight.c program(Section 2.4), which spaces are essential?
 *
 * ---
 *
 * 1. blank line after the derectives(2.2.1)
 * 2. spaces after `#define`. e.g. `#define INCHES_PER_POUND 166` can't remove space to `#defineINCHES_PER_POUND166`
 * 3. spaces around keyword. e.g. `int main` can't remove space to `intmain`, `return 0` can't remove space to `return0`
 */

